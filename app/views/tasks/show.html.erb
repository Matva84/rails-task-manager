<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= if (@task.completed == true)
str = "This task is completed"
else
str = "This task is not completed"
end%>
<p></p>
<%= link_to("Back to list", index_path) %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
